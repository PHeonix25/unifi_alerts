# Configures GitHub's `--generate-notes` so the auto-generated release notes
# group merged PRs by category instead of dumping a flat list. Categories are
# matched by PR labels; PRs with no matching label fall through to "Other".
#
# The labels referenced below MUST exist on the repository for grouping to
# work. Run `scripts/setup-labels.sh` once per fork to create the non-default
# labels (`security`, `tests`, `ci`, `github-actions`, `dependencies`). The
# `bug`, `enhancement`, and `documentation` labels are GitHub defaults.
#
# Pair this file with `gh release create --generate-notes` in
# `.github/workflows/release.yml` so each release surfaces a categorised
# summary scoped to commits between the previous tag and the current one.
changelog:
  exclude:
    labels:
      - "ignore-for-release"
      - "duplicate"
      - "wontfix"
    authors:
      - "dependabot"
  categories:
    - title: 🔒 Security
      labels:
        - "security"
    - title: 🐛 Bug Fixes
      labels:
        - "bug"
    - title: 🚀 Features
      labels:
        - "enhancement"
    - title: 📚 Documentation
      labels:
        - "documentation"
    - title: 🧪 Tests
      labels:
        - "tests"
    - title: ⚙️ CI / Release
      labels:
        - "ci"
        - "github-actions"
        - "dependencies"
    - title: 🧹 Other Changes
      labels:
        - "*"
