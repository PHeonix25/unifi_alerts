{
  "config": {
    "step": {
      "user": {
        "title": "Connect to UniFi Controller",
        "description": "Enter your UniFi Network controller address and credentials. The integration will auto-detect which method to use.\n\n**API key** (UniFi OS consoles — UDM Pro, UCG Ultra, etc.): leave Username and Password blank. Generate a key in **Settings → Admins & Users → API Keys**.\n\n**Username / password** (older controllers or Cloud Key): fill in Username and Password, leave API Key blank.\n\n⚠️ SSL verification is disabled by default — appropriate for controllers with self-signed certificates. Enable it if your controller has a trusted certificate.",
        "data": {
          "controller_url": "Controller URL (e.g. https://192.168.1.1)",
          "username": "Username (leave blank if using API key)",
          "password": "Password",
          "api_key": "API Key (UniFi OS only — leave blank if using username/password)",
          "verify_ssl": "Verify SSL certificate"
        }
      },
      "categories": {
        "title": "Configure Alert Categories",
        "description": "Choose which alert types to monitor. Each enabled category gets its own sensors and a dedicated webhook URL (shown in the next step).\n\n⚠️ **Network: Client connect/disconnect** and **Network: Device offline/online** fire very frequently on active home networks. Leave these OFF unless you specifically need them.\n\n**Polling interval**: how often to fetch open-alarm counts from the controller as a backup to webhooks (lower = more accurate, higher = less load).\n**Auto-clear timeout**: minutes before an active sensor automatically resets to OFF if no further alert arrives.",
        "data": {
          "cat_network_device": "Network: Device offline/online",
          "cat_network_wan": "Network: WAN offline/latency",
          "cat_network_client": "Network: Client connect/disconnect",
          "cat_security_threat": "Security: Threat / IDS detected",
          "cat_security_honeypot": "Security: Honeypot triggered",
          "cat_security_firewall": "Security: Firewall block",
          "cat_power": "Power: PoE / power loss",
          "poll_interval": "Polling interval (seconds)",
          "clear_timeout": "Auto-clear timeout (minutes)"
        }
      },
      "finish": {
        "title": "Webhook URLs",
        "description": "Copy these webhook URLs into UniFi Alarm Manager — one per enabled category — then click **Submit** to save the integration.\n\n{webhook_url_list}\n\nYou can always retrieve these URLs later via **Settings → Devices & Services → UniFi Alerts → Configure**."
      }
    },
    "error": {
      "cannot_connect": "Cannot reach the controller. Check the URL and that the controller is online.",
      "invalid_auth": "Invalid credentials. Check your username/password or API key.",
      "unknown": "An unexpected error occurred. Check the Home Assistant logs."
    },
    "abort": {
      "already_configured": "This UniFi controller is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Update Alert Categories",
        "description": "Current webhook URLs for your enabled categories:\n\n{webhook_url_list}",
        "data": {
          "cat_network_device": "Network: Device offline/online",
          "cat_network_wan": "Network: WAN offline/latency",
          "cat_network_client": "Network: Client connect/disconnect",
          "cat_security_threat": "Security: Threat / IDS detected",
          "cat_security_honeypot": "Security: Honeypot triggered",
          "cat_security_firewall": "Security: Firewall block",
          "cat_power": "Power: PoE / power loss",
          "poll_interval": "Polling interval (seconds)",
          "clear_timeout": "Auto-clear timeout (minutes)"
        }
      }
    }
  }
}
